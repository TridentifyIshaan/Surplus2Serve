# MongoDB Integration Requirements for Surplus2Serve
# Install with: pip install -r requirements-mongodb.txt

# Core MongoDB dependencies
motor==3.3.2                    # Async MongoDB driver for Python
pymongo==4.6.0                  # MongoDB Python driver (required by motor)
dnspython==2.4.2                # For MongoDB Atlas connection strings

# Authentication and security
bcrypt==4.1.2                   # Password hashing
PyJWT==2.8.0                    # JSON Web Tokens for authentication
python-multipart==0.0.6         # Required for file uploads and form data

# Email validation
email-validator==2.1.0          # Email validation for Pydantic

# Optional: Environment variable management
python-dotenv==1.0.0            # Load environment variables from .env file

# Development and testing (optional)
pytest-asyncio==0.23.0          # Async testing support
httpx==0.26.0                   # Async HTTP client for testing
